#include<bits/stdc++.h>
using namespace std;
int main(){
	int n;
	scanf("%d",&n);
	vector<int> A,B;
	A.resize(n);
	B.resize(n);
	
	for(int i=0; i<n; i++)
		scanf("%d", &A[i]);
	
	for(int i=0; i<n; i++)
		scanf("%d", &B[i]);
		
	do{
		
		
		
		
	}while(next_permutation(A,A+n));
	
	return 0;
}